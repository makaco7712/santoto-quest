# Audio

La música actual es chiptune generado con Web Audio API
(`src/systems/AudioSystem.ts`). Para usar audio real, coloca aquí
`titulo.ogg`, `overworld.ogg`, `duelo.ogg` y sfx, cárgalos en
PreloadScene y reemplaza las llamadas a AudioSystem.
